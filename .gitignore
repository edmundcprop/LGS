node_modules
.next
out
.DS_Store
*.log
.env
.env*.local

# Local Netlify folder
.netlify

# Local-only build/deploy artifacts
deploy-*.zip
deno.lock
tsconfig.tsbuildinfo

# Claude Code local config (machine-specific tool permission allowlist)
.claude/settings.local.json

# Office app lockfiles (LibreOffice, Word, Excel temp lock indicators)
.~lock.*#
~$*
